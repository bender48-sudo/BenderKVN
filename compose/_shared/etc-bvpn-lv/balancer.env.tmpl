# BenderVPN balancer secrets
# Загружается в /opt/scripts/balancer.sh через source
# Права: 600 root:root
BOT_TOKEN=${BOT_TOKEN}
PANEL_TOKEN=${REMNA_API_TOKEN_LV}
REMNA_API_TOKEN=${REMNA_API_TOKEN_LV}
# Публичный URL панели (без завершающего /). Обязательно после переноса панели с LV на AMS.
PANEL_URL=https://k9x2m1.conntest.xyz:8443
# Infra/monitor alerts → private ops channel (not owner DM / user bot chat)
OPS_ALERT_CHAT_ID=${OPS_ALERT_CHAT_ID}
# Legacy fallback if OPS_ALERT_CHAT_ID unset (migration); shop bot uses ADMIN_TELEGRAM_ID separately on AMS
ADMIN_CHAT_ID=${ADMIN_CHAT_ID:-924498094}
