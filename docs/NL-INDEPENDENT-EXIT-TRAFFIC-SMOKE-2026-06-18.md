# NL Independent Exit Traffic Smoke — 2026-06-18

**Task:** NL-INDEPENDENT-EXIT-TRAFFIC-SMOKE-AND-PROMOTION-001  
**Status:** **FAIL_PROFILE_ROUTING_PROTOCOL → WAITING_RETEST** — owner ran legacy single profile; corrected two-profile flow ready locally.  
**Fix task:** [`NL-INDEPENDENT-EXIT-FIX-2026-06-18.md`](NL-INDEPENDENT-EXIT-FIX-2026-06-18.md)  
**Companion:** [`NEW-INDEPENDENT-EXIT-PATH-2026-06-17.md`](NEW-INDEPENDENT-EXIT-PATH-2026-06-17.md)

> Owner/staging traffic smoke only. No prod apply, no PUBLIC_PROD, no subscription/
> template mutation. IPs/secrets redacted (node IDs / ep_<hash> only).

---

## 1. Baseline (preflight)

| Metric | Value |
|--------|-------|
| Branch | `product-referral-cabinet-ui-v1` |
| `delivery_path_nodes` | **1** |
| `independent_exit_paths` | **1** (lv-exit-1 only) |
| `nl-node-1` status | staging · `independent_exit_candidate` · `acceptance_status=pending_traffic_smoke` |
| `delivery_path_eligible` | **false** (not counted) |
| PUBLIC_PROD | **NO-GO** · APPLY_ALLOWED=false |
| CANARY dry-run (live registry) | GO (lv-exit-1 only; NL excluded) |

---

## 2. Owner smoke artifact (two-profile flow)

| Path | Purpose | Import? |
|------|---------|---------|
| `.local/independent_exit_nl_canary_RUNBOOK.md` | Instructions only | **NO** |
| `.local/independent_exit_nl_canary_METADATA.json` | Scorecard/checklist | **NO** |
| `.local/independent_exit_nl_DIRECT_BASIC_IMPORTABLE_PROFILE.json` | Phase 1 — NL connectivity proof | **YES (first)** |
| `.local/independent_exit_nl_SPLIT_STEALTH_IMPORTABLE_PROFILE.json` | Phase 2 — NL + relay-2 stealth | **YES (after Phase 1 PASS)** |
| `.local/independent_exit_nl_canary_IMPORTABLE_PROFILE.json` | Legacy single profile (bug) | **DEPRECATED** |

Regenerated by `ops/generate_independent_exit_canary.py` with:

- two-phase import instructions (DIRECT_BASIC → SPLIT_STEALTH);
- disable Happ external routing profile overlay;
- split smoke checklists aligned with routing;
- promotion plan **documented but not applied** until owner reports PASS on both phases.

---

## 3. Traffic smoke result

| Field | Value |
|-------|-------|
| **Verdict** | **FAIL_PROFILE_ROUTING_PROTOCOL** (legacy profile); **WAITING_RETEST** (corrected profiles) |
| Evidence | Owner imported legacy single profile; `geosite:google` → Intl_Stealth; Gmail did not prove NL. report(11) analyzed locally. |
| Registry promotion | **Not performed** |

---

## 4. NL status before / after

| Field | Before | After |
|-------|--------|-------|
| `status` | staging | **staging** (unchanged) |
| `acceptance_status` | pending_traffic_smoke | **pending_traffic_smoke** (unchanged) |
| `delivery_path_eligible` | false | **false** (unchanged) |
| `canary_percent` | 0 | **0** (unchanged) |
| `independent_exit_paths` | 1 | **1** (unchanged) |

---

## 5. Promotion plan (on owner PASS only — not applied)

Conservative repo-side promotion (separate commit after owner PASS on **both** profile phases):

- `acceptance_status=traffic_smoke_pass`
- `status=canary`
- `canary_percent=5` (owner/staging cohort only)
- `allow_new_assignments=false`
- `delivery_path_eligible=true` (canary context — **not** PUBLIC_PROD apply)
- PUBLIC_PROD remains NO-GO until separate gate + owner APPROVE PUBLIC PROD APPLY

---

## 6. Owner runbook (exact steps)

1. `python ops/generate_independent_exit_canary.py` (regenerate `.local` artifacts).
2. Import **DIRECT_BASIC** as a **new** Happ profile; auto-refresh OFF; **disable external routing profile**.
3. Run Phase 1 checklist (Google/Gmail NL proof, 10–15 min; no Telegram).
4. On PASS → import **SPLIT_STEALTH**; run Phase 2 checklist (TG/Meta stealth + NL catch-all).
5. Fill `record_template` in local metadata copy.
6. Reply with `overall_verdict: PASS|PARTIAL|FAIL` + redacted notes.

---

## 7. Gate update

| Gate | Status |
|------|--------|
| LAB_OWNER | GO |
| NL architecture validated | GO |
| NL traffic smoke | **FAIL / WAITING_RETEST** |
| CANARY (controlled) | NO-GO until smoke PASS + promotion |
| PUBLIC_PROD | NO-GO |
| Paid beta / commercial | NO-GO |
| 300 / 30k | NO-GO |

---

## 8. Next owner action

**RUN OWNER NL DIRECT BASIC SMOKE** — import `.local/independent_exit_nl_DIRECT_BASIC_IMPORTABLE_PROFILE.json` only.

---

## 9. Safety statement

| Item | Value |
|------|-------|
| Prod mutation | **NO** |
| Deploy/reload | **NO** |
| Remna/Caddy/template/subscription | **NO** |
| Live VPN routing changes | **NO** |
| LV preserved | **YES** |
| Capacity inflated | **NO** |
| Raw logs/secrets committed | **NO** |
| NL PASS claimed | **NO** |
| 300/30k GO claimed | **NO** |
