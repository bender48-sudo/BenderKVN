# Git и GitHub (коротко)

Рабочая копия **SoT по коду**: **`d:\Va\projects\VPN`**, ветка **`main`**, удалённый **`origin`** — репозиторий **https://github.com/VaDi-ai/BenderKVN** (см. **`README.md`** в корне).

**Операционная память проекта** — **`docs/KNOWLEDGE-BASE.md`**; бэклог и журнал — **`docs/COMMERCIAL-BACKLOG.md`** (§12).

---

## Практика

1. Секреты и тяжёлые артефакты не коммитить — см. **`.gitignore`**, **`docs/SECRETS.md`**.  
2. После ручных правок на проде — **`python ops/drift-check.py`** с машины, где настроен SSH к **LV/AMS/NL**, и при необходимости перенос изменений в репо.  
3. Не полагаться только на «ветки на VPS» — история должна жить в **`git log`** на GitHub/локально.

---

## Историческая справка (до появления `.git`)

Изначально дерево **`d:\Va\projects\VPN`** жило без **`.git`**; репозиторий был поднят в **май 2026** (перенос в **BenderKVN**). Старые локальные архивы без Git, если остались, не считать каноном.
